# Vital-Signal-Viewer

## Running the Project

1. **Prerequisites**:
   - Python (version X.X)
   - PyQt5 (version X.X)

2. **Installation**:
   To install the required dependencies, run the following command:
   ```bash
   pip install PyQt5
